name: Deploy Nexus Backend
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Debug
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            docker logs nexus-celery-agent-worker-1 --tail 20 2>&1
            echo "=== DB check ==="
            docker exec nexus-mysql-1 mysql -u nexus -pnexus_secret nexus_core -e "SELECT id, status, confidence_score FROM reports_researchreport ORDER BY created_at DESC LIMIT 3;" 2>/dev/null
